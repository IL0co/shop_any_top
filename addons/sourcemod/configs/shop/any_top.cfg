"Any Top" 
{
    "most_expensive_inventory" 
    {
        "DataBase Query"        "SELECT name, SUM(`buy_price`) as `total` FROM `{prefix}boughts` LEFT JOIN `{prefix}players` WHERE `player_id` = `id` AND `buy_price` > '0' GROUP BY `player_id` ORDER BY `total` DESC LIMIT 10;"
        // "#format"               "{1:0},{2:}"
        "Item Format"           "{id}. {1} - {2}"
        "Menu Name"             ""
        "Menu Tittle"           ""
        "Update Time"           "120"   
    }
}